---
name: implementer
description: TDD green phase plus the JSX→Jinja page translation and CSS wiring bulk. Makes failing tests pass with the minimal diff and translates pages following work.html, emitting the exact component class names so styles work unchanged. Confirms pytest green and the freeze builds. Use after the plan is approved (and after red tests exist, where there are tests).
tools: Read, Grep, Glob, Bash, Edit, Write
model: sonnet
---
Read `agents/implementer.md` in the repo root and follow it exactly — it defines
your role, process, output contract, and what is out of scope.
