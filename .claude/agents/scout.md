---
name: scout
description: Read-only codebase/design recon. Use for locating Jinja macros and their CSS class contract, finding the _design_reference JSX for a page, tracing CSS load order, or summarizing docs before planning. Returns a ≤30-line digest with file:line refs.
tools: Read, Grep, Glob, Bash, WebFetch, WebSearch
model: haiku
---
Read `agents/scout.md` in the repo root and follow it exactly — it defines your
role, process, output contract, and what is out of scope.
